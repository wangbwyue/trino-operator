---
# Source: trino-operator/templates/deploy.yaml
apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: controller-manager
  name: trino-system
---
# Source: trino-operator/templates/deploy.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: trino-controller-manager
  namespace: trino-system
---
# Source: trino-operator/templates/tarim.deepexi.com_trinos.yaml
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: trinos.tarim.deepexi.com
spec:
  group: tarim.deepexi.com
  names:
    kind: Trino
    listKind: TrinoList
    plural: trinos
    singular: trino
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Trino is the Schema for the trinoes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TrinoSpec defines the desired state of Trino
            properties:
              cataLogConfig:
                additionalProperties:
                  type: string
                description: catalog config
                type: object
              coordinatorConfig:
                description: coordiator config
                properties:
                  configProperties:
                    description: config properties
                    type: string
                  jvmConfig:
                    description: jvm config
                    type: string
                  logProperties:
                    description: log properties
                    type: string
                  nodeProperties:
                    description: node prpperties
                    type: string
                required:
                - configProperties
                - jvmConfig
                - logProperties
                - nodeProperties
                type: object
              coordinatorCpu:
                description: Coordinator cpu
                type: integer
              coordinatorMemory:
                description: Coordinator mem
                type: integer
              cpuTotal:
                format: int64
                type: integer
              createTime:
                format: date-time
                type: string
              memoryTotal:
                format: int64
                type: integer
              metastore:
                description: metastore
                type: string
              pause:
                description: Pause
                type: boolean
              projectSpeaceId:
                description: projectSpeaceId
                type: integer
              tenantId:
                description: TenantId
                type: string
              workConfig:
                description: work config
                properties:
                  configProperties:
                    description: config properties
                    type: string
                  jvmConfig:
                    description: jvm config
                    type: string
                  logProperties:
                    description: log properties
                    type: string
                  nodeProperties:
                    description: node prpperties
                    type: string
                required:
                - configProperties
                - jvmConfig
                - logProperties
                - nodeProperties
                type: object
              workCpu:
                description: per work cpu
                type: integer
              workMemory:
                description: per work memory
                type: integer
              workNum:
                description: WorkNum
                type: integer
            required:
            - cataLogConfig
            - coordinatorConfig
            - pause
            - projectSpeaceId
            - workConfig
            - workCpu
            - workMemory
            - workNum
            type: object
          status:
            description: TrinoStatus defines the observed state of Trino
            properties:
              coordinatorPod:
                description: Coordinator
                items:
                  properties:
                    cpu:
                      type: string
                    memory:
                      type: string
                    name:
                      type: string
                    podStatus:
                      type: string
                  required:
                  - cpu
                  - memory
                  - name
                  - podStatus
                  type: object
                type: array
              status:
                description: status
                type: string
              workerPod:
                description: Worker
                items:
                  properties:
                    cpu:
                      type: string
                    memory:
                      type: string
                    name:
                      type: string
                    podStatus:
                      type: string
                  required:
                  - cpu
                  - memory
                  - name
                  - podStatus
                  type: object
                type: array
            required:
            - coordinatorPod
            - status
            - workerPod
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
---
# Source: trino-operator/templates/deploy.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: trino-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: "cluster-admin"
subjects:
- kind: ServiceAccount
  name: trino-controller-manager
  namespace: trino-system
---
# Source: trino-operator/templates/deploy.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    control-plane: controller-manager
  name: trino-controller-manager
  namespace: trino-system
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    metadata:
      labels:
        control-plane: controller-manager
    spec:
      containers:
      - args:
        - --secure-listen-address=0.0.0.0:8443
        - --upstream=http://127.0.0.1:8080/
        - --logtostderr=true
        - --v=10
        image: gcr.io/kubebuilder/kube-rbac-proxy:v0.8.0
        name: kube-rbac-proxy
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8443
          name: https
      - args:
        - --health-probe-bind-address=:8081
        - --metrics-bind-address=127.0.0.1:8080
        - --leader-elect
        command:
        - /manager
        image: deploy.deepexi.com/fastdata/trino-operator:v3
        imagePullPolicy: Always
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        name: manager
        ports:
        - containerPort: 9443
          name: webhook-server
          protocol: TCP
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
      securityContext:
        runAsNonRoot: true
      serviceAccountName: trino-controller-manager
      terminationGracePeriodSeconds: 10
      volumes:
      - name: cert
        secret:
          defaultMode: 420
          secretName: webhook-server-cert
